# src/lib/

**Parent:** [app-ai/CLAUDE.md](../../CLAUDE.md)

Shared library modules consumed by the handlers and the router. The Anthropic API
key is read only inside `anthropic.ts` (via `claudeApiKey(env)` from
`../config.ts` — the single typed-Env binding reader) and never leaves the
worker. `anthropic.ts` also owns the shared Anthropic Messages wire types
(content blocks, payloads, `CacheControl`) consumed across handlers and the
cache modules.

## Files

| File                     | Exports                                                                                                                                                           | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `anthropic.ts`           | `uploadFile`, `listFiles`, `getFileMetadata`, `deleteFile`, `createMessage`, `streamMessage`, `countTokens`, `supportsEffort`                                     | The ONE place that talks to `api.anthropic.com`. Owns base URLs, `anthropic-version`, beta flags, and `x-api-key` injection. `getFileMetadata(id)` GETs `/v1/files/{id}` (200 alive / 404 gone) — used by dedup to verify a cached id before reuse and by `chat.ts` to type file blocks by mime. `supportsEffort(model)` is the effort-capability gate (Fable 5, Opus 4.8/4.7/4.6/4.5, Sonnet 5/4.6) — handlers use it before attaching `output_config.effort` so unsupported models (Haiku 4.5, Sonnet 4.5) don't 400.                                                                                                                                                                                                                                                                                                                                      |
| `auth.ts`                | `validateContextId`                                                                                                                                               | Validate `X-Personalizer-Context-ID` server→server against Personalizer. Throws a `WorkerError` on any failure: missing header → 401 `MissingContextIDException`; a Personalizer rejection carries Personalizer's status + body for verbatim passthrough; an unreachable Personalizer → 500 `BrainUnreachableException`. On success resolves the parsed `ContextValidation` payload — which carries `AvailableIntegrationParties` (the subscriber's liveness-filtered `IntegrationParty` names); the router threads it into `/chat` for dynamic toolset composition (no extra call, no cache).                                                                                                                                                                                                                                                               |
| `cors.ts`                | `getCorsHeaders`                                                                                                                                                  | Build CORS headers from the request Origin. Echoes any merchant Origin without credentials; the `CREDENTIALED_ORIGINS` config variable (parsed by `../config.ts`) allowlists the dev origins that may send credentialed requests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `responses.ts`           | `jsonResponse`, `errorResponse`, `errorPayload`, `errorResponseFrom`, `anthropicErrorResponse`, `WorkerError`                                                     | The ONE error format: Brain's wire shape (`{ Message, ExceptionType, MessageDetail? }`, flat PascalCase, `MessageDetail` omitted when absent) — app-ai errors are structurally identical to Brain errors so clients keep one parser. `WorkerError` pins status/`ExceptionType`/detail at the throw site (and `passthroughBody` relays a forwarded Brain error verbatim); `errorResponseFrom` maps any caught error to the envelope; `anthropicErrorResponse` wraps a non-ok Anthropic body (`AnthropicApiException`, upstream status kept, raw body in `MessageDetail`).                                                                                                                                                                                                                                                                                     |
| `cache-control.ts`       | `manageCacheControl`, `EXTENDED_CACHE_CONTROL`, `STABLE_CACHE_PREFIX_MARKER`                                                                                      | Keep `/messages` within Anthropic's 4-block `cache_control` limit; the 1h extended-TTL breakpoint const for stable prefixes. Counts the actual cached system and attachment blocks, strips unmanaged client breakpoints outside the proxy-owned attachment prefix, gives a caller-marked terminal stable user-prefix block priority over normally ranked user images/documents, strips the internal `limespot_stable_cache_prefix` marker before Anthropic forwarding, and preserves content order/file IDs.                                                                                                                                                                                                                                                                                                                                                 |
| `brain-defaults.ts`      | `getDefaultsBlock`, `projectDefaults`, `canonicalStringify`, `resetDefaultsCacheForTests`, `ProjectedDefaults`, `ProjectedBoxDefaults`, `ProjectedMobileDefaults` | Global defaults provider for the PROPOSE prompts (`injectsBrainDefaults` entries). Fetches canonical Brain `RecommendationsSettings` (`GET ${PERSONALIZER_API_URL}/v1/personalizerConfig?defaultRecommendationsSettings=true`, forwarding the context-ID; or a configured CDN URL via `recommendationsDefaultsUrl`, fetched without the context-ID), projects only the AI-relevant fields while preserving page -> box hierarchy and desktop/mobile inheritance, and emits compact byte-stable canonical JSON. One global in-isolate promise/result coalesces cold requests and keeps a successful block for the isolate; a cold failure skips injection and remains retryable. There is no defaults-version protocol. `messages.ts` injects the stable block as a second cacheable system block after the registry prompt and before per-store screenshots. |
| `agent-cache.ts`         | `applyConversationBreakpoints`                                                                                                                                    | Place cache breakpoints on the growing `/chat` conversation turns (last block + ~every-15-blocks), re-stripped each iteration. `maxBreakpoints` (default 3) + `reservedHeadBlocks` (protects the placement file prefix's own 1h breakpoint) keep the total ≤4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `file-dedup.ts`          | `dedupUpload`, `sha256Hex`, `KV_RECORD_TTL_SECONDS`                                                                                                               | Files API content-hash dedup: SHA-256 → `file_id` via the `FILES_KV` binding (bound in prod → LIVE). Verifies a cached id still exists before reuse (stale → re-upload); records carry a 30-day TTL. Graceful no-op (plain upload) when KV is unbound.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `references.ts`          | `sanitizeRefs`, `refCategoryPath`, `resolveRef`, `buildReferencedEntitiesBlock`, `MAX_CHAT_REFS`, `RECORD_REF_TYPES`, `ANALYTICS_REF_TYPES`                       | Chat referencing (`context.refs`, frozen contract in lib CONTRACTS.md §1/§8). Intake sanitizer (valid type literals only, record refs need an `id`, cap 5 — first five kept) + the eager Referenced-Entities system block: every surviving ref resolves concurrently via `Promise.allSettled` (record types → `fetchEntityContext`, the personalizer toolset's normalizer in `../toolsets/personalizer/entity-context.ts`, sharing ONE subscriber lookup per block build; analytics types → the ref's own `metadata`, NO Personalizer call); a failed resolve degrades that ref's line to `(could not load)` — the block never throws. The block text is a frozen format: `## Referenced Entities` heading + preamble + one `- [<type>] "<label>" (<anchor>): <payload>` line per ref.                                                                       |
| `dev-inference-trace.ts` | `createDevelopmentInferenceTrace`, `traceableContent`, `traceableMessages`, `traceableSystem`, `DevelopmentInferenceTrace`                                        | Structured inference trace producer. Correlates complete model-visible `/messages` and each `/chat` agent-loop turn—including file references—with assistant output, client/server tool decisions, execution results, completion/failure, and elapsed time. Emits at TRACE through the request logger, so deployment configuration owns all sink policy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `logger.ts`              | `createLoggingRuntime`, `silentLogger`, `silentLoggingRuntime`, `toLogValue`, `Logger`, `LoggingRuntime`, `LogContext`, `BackgroundTaskScheduler`                 | Request-scoped structured logging runtime. Parses every `LOG_TARGETS_*` route once, fans each record to zero/one/multiple registered sinks, delivers CLEF asynchronously to Seq with optional `LOG_SEQ_API_KEY`, captures background rejections, preserves error stacks/causes/details and context IDs, redacts actual secret-bearing fields, and keeps sink failures fail-open/non-recursive. The Console sink is the only Worker source allowed to call `console.*`.                                                                                                                                                                                                                                                                                                                                                                                       |

## Conventions

- Every exported function and type carries a doc comment (TSDoc; types are in the signatures).
- Cache/effort/dedup levers are **additive** — they never change external
  request/response shapes (see the token-saving section in the root CLAUDE.md).
- Beta flags live only in `anthropic.ts`: Files calls send `files-api-2025-04-14`;
  Messages calls send `prompt-caching-2024-07-31,files-api-2025-04-14`.
