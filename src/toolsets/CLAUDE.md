# src/toolsets/

**Parent:** [app-ai/CLAUDE.md](../../CLAUDE.md) · Standards: [docs/TOOLSETS.md](../../docs/TOOLSETS.md)

The worker's AI-tool layer: named TOOLSETS (one directory per backend) plus
the registry that composes them per request. Handlers consume tools ONLY
through the registry — each endpoint passes the subscriber's available
integration parties (`handlers/chat.ts` takes `availableParties`, threaded from
the router's validate-context-id call) and treats the composition
(`{ names, definitions, execute }`) as its whole tool surface. Composition is
data-driven off each descriptor's `integrationParties` gate (empty =
always-active); adding a toolset never edits handler logic.

## Files

| File                             | Exports                                                                                                                                                     | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `registry.ts`                    | `composeToolsets`                                                                                                                                           | Compose the active toolsets for one request from the subscriber's available `IntegrationParty` set + the request's auth context (`{ contextId, env }`). A toolset is offered iff its `integrationParties` is empty (always-active) OR intersects the subscriber's parties; each gated toolset's MATCHED party (exactly one, by the ≤1-commerce-party invariant — defensive assert, no tiebreak) is threaded to `execute` as `matchedParty`. Returns `{ names, definitions, execute }` in registered order. Routes each `tool_use` to the owning toolset; a gated-off or unowned tool name degrades to the frozen `Unknown tool` shape. Resolves per-toolset credentials lazily, memoized per composition (per-request scope only). Throws at compose time on a cross-toolset tool-name collision; a party no toolset declares is ignored.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `types.ts`                       | `ToolsetDescriptor`, `ToolsetComposition`, `ToolExecutionResult`, `ToolAuthContext`, `ToolExecutionContext`, `ToolCallExtras`                               | The toolset seam TYPES — the descriptor contract every toolset implements (`name` / `integrationParties` / `definitions` / `resolveCredentials` / `execute`), the composition the registry hands to handlers, and the frozen `{ ok, name, result, summary }` execution shape (`ToolExecutionContext` also carries the gated toolset's `matchedParty`). Handlers and toolsets both import from here, so the seam is typed once.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `integration-party.ts`           | `IntegrationParty`                                                                                                                                          | Verbatim mirror of Personalizer's C# `IntegrationParty` enum (member NAMES; the wire form, never the guid). The single identity keying composition: descriptors gate on it, validate-context-id returns the available set, the executor builds the bridge URL segment from the matched party. v1-active: `ShopifyPersonalizer`, `BigCommercePersonalizer`, `Klaviyo`, `Google`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `integration-bridge.ts`          | `resolveBridgeChannel`, `executeRestRequest`, `executeGraphQlRequest`, `executeGaqlRequest`, `unknownToolResult` (+ the `BridgeChannel` / tool-input types) | Shared plumbing behind the platform toolsets. `resolveBridgeChannel({ contextId, env })` is every platform toolset's credential seam — it resolves the CALL CHANNEL toward Personalizer's integration bridge (`{ contextId, serviceToken, baseUrl }` via the `personalizerIntegrationBridgeToken` / `personalizerApiUrl` accessors in `src/config.ts`; app-ai never holds a platform token), thrown failures degrade via the registry. The executors map tool input onto the bridge wire shapes, where `{party}` is the matched `IntegrationParty` name (`{verb} v2/integration-bridge/{party}/rest/{path}?{query}` with JSON POST/PUT bodies and body-less DELETE; `POST …/{party}/graphql` with `{ Query, Variables? }`; `POST …/{party}/gaql` with `{ Query, PageSize?, PageToken? }` — PascalCase, Personalizer's request models; the GAQL response `NextPageToken` surfaces verbatim so the model pages by echoing it back), send both channel headers (`X-Personalizer-Context-ID` + `X-Personalizer-Integration-Bridge-Token`), and discriminate responses by the `X-Ls-Integration-Bridge-Error` header: absent → the platform speaking, surfaced verbatim as `{ ok: true, result: { platformStatus, headers?, body } }` at ANY status (platform 4xx/5xx are data the model self-corrects on; `headers`, lowercased keys, is the whitelisted out-of-band subset — `Link` for Shopify REST `page_info` cursors, `Retry-After`, the platform rate-limit headers — omitted when none are present); present → the LimeSpot `{ Message, ExceptionType }` envelope, degraded to `{ ok: false, result: { error: '<Message> (<ExceptionType>)' } }`. Never throws toward the loop; never logs the channel. |
| `shopify/index.ts`               | `shopifyToolset`                                                                                                                                            | Shopify platform toolset (`shopify_rest_request` — version-less Admin REST paths, Personalizer pins the API version; `shopify_graphql` — documents forwarded verbatim, mutations included). Dispatches through `integration-bridge.ts` onto the `shopify` proxy segment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `bigcommerce/index.ts`           | `bigcommerceToolset`                                                                                                                                        | BigCommerce platform toolset (`bigcommerce_rest_request` — version-PREFIXED `v2/…`/`v3/…` paths, the tree's own convention; v3 `meta.pagination` passes through in the platform body). Dispatches through `integration-bridge.ts` onto the `bigcommerce` proxy segment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `klaviyo/index.ts`               | `klaviyoToolset`                                                                                                                                            | Klaviyo platform toolset (`klaviyo_rest_request` — `api/…` paths; JSON:API paging is body-native, cursors follow via the `page[cursor]` query param). Dispatches through `integration-bridge.ts` onto the `klaviyo` proxy segment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `google-ads/index.ts`            | `googleAdsToolset`                                                                                                                                          | Google Ads platform toolset (`google_ads_gaql_query` — GAQL SELECT against the merchant's linked ads account, which Personalizer resolves itself; read-only by nature of GAQL). Dispatches through `integration-bridge.ts` onto the `google` proxy segment (gaql surface).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `personalizer/index.ts`          | `personalizerToolset`                                                                                                                                       | The personalizer toolset descriptor — every tool whose backend is the Personalizer API: the frozen Anthropic tool definitions (`get_store_analytics`, `list_segments`, `list_campaigns`, `get_store_config`, `get_entity_context` — lib CONTRACTS.md §2), the `/v2/ai-tools/*` dispatch (server→server, forwarding the merchant's context-ID), the `get_entity_context` dispatch table (record → `entity-context.ts`, analytics → the request's `refs`, no Personalizer call), and the `{ ok, name, result, summary }` degrade convention. Credentials seam: `resolveCredentials({ contextId }) → { contextId }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `personalizer/entity-context.ts` | `fetchEntityContext`                                                                                                                                        | The personalizer toolset's normalizer: record-ref resolution (`campaign` / `segment` / `progress-bar` / `bundle`) → the frozen model-visible `AiToolEntityContext` shape (`{ Type, Id, Found, Title, Status, Kind, Summary, Fields, Related }`, null members omitted, not-found → `{ Type, Id, Found: false }`). Per-type fetcher table over Personalizer's per-record admin endpoints, forwarding the context-ID: campaign probes discount → html → image (`v2/discount-campaigns/discount-campaign/{guid}` → `v2/html-campaigns/html-campaign/{guid}` → `v1/imageCampaigns?guid=`), segment → `v1/subscriberSegment?guid=`, progress-bar → `v2/progress-bar-campaigns/{guid}`, bundle → the discount route with a bundle-flavored Summary. Every resolution also loads `v2/accounts/subscriber` (memoizable via the optional `shared` param): its Guid is the tenant re-check on every loaded entity, its CurrencyCode fills the progress-bar `Currency` field. Discount/bundle `Related` = one-hop targeted segments via per-guid `v1/subscriberSegment` loads (tenant-checked, non-deleted). Misses (404/204/5xx/cross-subscriber/deleted) → `Found: false`; malformed guid, unknown type, subscriber-lookup failure, 401/403, network failure → throw (callers degrade). Also consumed by `lib/references.ts` for the eager Referenced-Entities block. No extra context-ID validation call — Personalizer re-validates the forwarded header on every proxied call.                                                                                                                                                                                                                                    |

## Conventions

- **Descriptor contract** (`name` / `definitions` / `resolveCredentials` /
  `execute`) and the full how-to-add-a-toolset guide live in
  [docs/TOOLSETS.md](../../docs/TOOLSETS.md).
- **Degrade, never throw:** every `execute` resolves to
  `{ ok, name, result, summary }` so the agent loop can feed failures back to
  the model (`tool_result` with `is_error`) instead of aborting the turn. Tool
  input is untrusted despite the frozen schema, so the platform executors guard
  their required fields (`method`/`path` for REST, `query` for GraphQL/GAQL)
  and degrade to an `Invalid input: …` result — a clean local signal the model
  can act on, and no malformed proxy round-trip (e.g. a `path`-less REST call
  becoming `/rest/undefined`; an array is not mistaken for an object). The REST
  executor also normalizes sloppy input that is still well-formed enough to
  send: leading slashes on `path` are stripped (no `rest//v3/...` double-slash
  misroute), a query string embedded in `path` is split off and merged with the
  `query` object (no malformed double-`?` URL), an array `query` is
  ignored rather than serialized as `?0=…&1=…`, and `null`/`undefined` query
  values are dropped rather than serialized as `"null"`/`"undefined"`; a
  lowercase `method` is uppercased so it still matches the body-forwarding check
  and reaches the platform as a standard HTTP verb.
- **Model-visible contract is frozen:** tool names, input schemas, and output
  shapes are pinned by the lib repo's `admin/ai/CONTRACTS.md` §2 and the
  contract snapshot in `test/toolsets.test.ts` — schema changes are contract
  changes.
- **Credentials:** resolved per request via `resolveCredentials`,
  subscriber-scoped, never model-visible, never logged; endpoints expose a
  toolset only when the subscriber's available parties satisfy its
  `integrationParties` gate (the five standards in docs/TOOLSETS.md).
- **Brain is the source of truth** for every shape this layer touches — the
  personalizer toolset's `/v2/ai-tools/*` + per-record admin shapes AND the
  AI integration-bridge wire contract consumed by `integration-bridge.ts` — mirror
  the sibling `brain` repo's C# exactly (root CLAUDE.md → Hard rules).
- **Platform toolsets never hold a platform token.** The `shopify` /
  `bigcommerce` / `klaviyo` / `google-ads` toolsets resolve only the call
  channel toward Personalizer's integration bridge (`integration-bridge.ts`); Personalizer is the
  credential custodian and relays each call under the merchant's own
  installed-integration credentials. The behavioral boundary is the tool
  descriptions' write-caution language plus the merchant token's OAuth scopes
  (docs/TOOLSETS.md → Platform toolsets).
- **Platform errors are data, not degrades.** The `X-Ls-Integration-Bridge-Error` header is
  the discrimination rule: absent → the platform's status + body surface
  verbatim to the model (`ok: true`, any status); present → the LimeSpot
  envelope degrades the call (`ok: false`).
- The platform toolsets are party-gated: `shopify` → `ShopifyPersonalizer`,
  `bigcommerce` → `BigCommercePersonalizer`, `klaviyo` → `Klaviyo`,
  `google-ads` → `Google`. A subscriber sees one only when that party is in
  their available set (from validate-context-id) — no endpoint allowlist.
