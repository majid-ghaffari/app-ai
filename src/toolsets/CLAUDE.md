# src/toolsets/

**Parent:** [app-ai/CLAUDE.md](../../CLAUDE.md) · Standards: [docs/TOOLSETS.md](../../docs/TOOLSETS.md)

The worker's AI-tool layer: named TOOLSETS (one directory per backend) plus
the registry that composes them per request. Handlers consume tools ONLY
through the registry — each endpoint declares an explicit toolset allowlist
(`CHAT_TOOLSETS = ['personalizer']` in `handlers/chat.ts`) and treats the
composition (`{ names, definitions, execute }`) as its whole tool surface.
Adding a toolset never edits handler logic.

## Files

| File                             | Exports                                                                                                                       | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `registry.ts`                    | `composeToolsets`                                                                                                             | Compose the active toolsets for one request from an endpoint allowlist + the request's auth context (`{ contextId, env }`). Returns `{ names, definitions, execute }`. Routes each `tool_use` to the owning toolset; unknown / out-of-allowlist tool names degrade to the frozen `Unknown tool` shape. Resolves per-toolset credentials lazily, memoized per composition (per-request scope only). Throws at compose time on an unregistered toolset name or a cross-toolset tool-name collision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `types.ts`                       | `ToolsetDescriptor`, `ToolsetComposition`, `ToolExecutionResult`, `ToolAuthContext`, `ToolExecutionContext`, `ToolCallExtras` | The toolset seam TYPES — the descriptor contract every toolset implements (`name` / `definitions` / `resolveCredentials` / `execute`), the composition the registry hands to handlers, and the frozen `{ ok, name, result, summary }` execution shape. Handlers and toolsets both import from here, so the seam is typed once.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `personalizer/index.ts`          | `personalizerToolset`                                                                                                         | The personalizer toolset descriptor — every tool whose backend is Brain (the Personalizer API): the frozen Anthropic tool definitions (`get_store_analytics`, `list_segments`, `list_campaigns`, `get_store_config`, `get_entity_context` — lib CONTRACTS.md §2), the `/v2/ai-tools/*` dispatch (server→server, forwarding the merchant's context-ID), the `get_entity_context` dispatch table (record → `entity-context.ts`, analytics → the request's `refs`, no Brain call), and the `{ ok, name, result, summary }` degrade convention. Credentials seam: `resolveCredentials({ contextId }) → { contextId }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `personalizer/entity-context.ts` | `fetchEntityContext`                                                                                                          | The personalizer toolset's normalizer: record-ref resolution (`campaign` / `segment` / `progress-bar` / `bundle`) → the frozen model-visible `AiToolEntityContext` shape (`{ Type, Id, Found, Title, Status, Kind, Summary, Fields, Related }`, null members omitted, not-found → `{ Type, Id, Found: false }`). Per-type fetcher table over Brain's per-record admin endpoints, forwarding the context-ID: campaign probes discount → html → image (`v2/discount-campaigns/discount-campaign/{guid}` → `v2/html-campaigns/html-campaign/{guid}` → `v1/imageCampaigns?guid=`), segment → `v1/subscriberSegment?guid=`, progress-bar → `v2/progress-bar-campaigns/{guid}`, bundle → the discount route with a bundle-flavored Summary. Every resolution also loads `v2/accounts/subscriber` (memoizable via the optional `shared` param): its Guid is the tenant re-check on every loaded entity, its CurrencyCode fills the progress-bar `Currency` field. Discount/bundle `Related` = one-hop targeted segments via per-guid `v1/subscriberSegment` loads (tenant-checked, non-deleted). Misses (404/204/5xx/cross-subscriber/deleted) → `Found: false`; malformed guid, unknown type, subscriber-lookup failure, 401/403, network failure → throw (callers degrade). Also consumed by `lib/references.ts` for the eager Referenced-Entities block. No extra context-ID validation call — Brain re-validates the forwarded header on every proxied call. |

## Conventions

- **Descriptor contract** (`name` / `definitions` / `resolveCredentials` /
  `execute`) and the full how-to-add-a-toolset guide live in
  [docs/TOOLSETS.md](../../docs/TOOLSETS.md).
- **Degrade, never throw:** every `execute` resolves to
  `{ ok, name, result, summary }` so the agent loop can feed failures back to
  the model (`tool_result` with `is_error`) instead of aborting the turn.
- **Model-visible contract is frozen:** tool names, input schemas, and output
  shapes are pinned by the lib repo's `admin/ai/CONTRACTS.md` §2 and the
  contract snapshot in `test/toolsets.test.ts` — schema changes are contract
  changes.
- **Credentials:** resolved per request via `resolveCredentials`,
  subscriber-scoped, never model-visible, never logged; endpoints expose
  toolsets only via explicit allowlists (the five standards in
  docs/TOOLSETS.md).
- **Brain is the source of truth** for every shape the personalizer toolset
  touches — mirror the sibling `brain` repo's C# exactly (root CLAUDE.md →
  Hard rules).
- Planned platform toolsets (`shopify-core`, `bigcommerce-core`, `google`,
  `klaviyo` — per-subscriber tokens from Brain's `SubscriberIntegrationConfig`)
  are pure additions through these same seams; the secure token-sharing
  mechanism is an open future design (docs/TOOLSETS.md → Roadmap seam).
